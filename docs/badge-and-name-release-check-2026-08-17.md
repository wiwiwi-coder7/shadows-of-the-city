# Badge and Character-name Release Check — 2026-08-17

The published `#/compare` page loads the new Case Badges panel successfully. A browser review initially showed legacy `ADRY` labels because the browser had cached the preceding hashed bundle. A cache-busted load of the current bundle confirmed the new output: scene titles and dialogue labels render as Lia, while the badge panel remains present and functional.
